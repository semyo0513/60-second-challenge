# 🎯 60초 미션 클리어 챌린지 — 독서행사 부스 게임 웹앱 기획서

> 독서 행사 부스에서 운영하는 60초 미션 게임. 관리자가 미션을 사전 등록 → 참가자가 랜덤으로 미션을 뽑음 → 60초 타이머가 화려하게 흐름 → 관리자가 클리어/실패 판정 → 랭킹보드 반영

---

## 0. 프로젝트 개요

| 항목 | 내용 |
|---|---|
| 용도 | 독서 행사 부스 체험 프로그램 |
| DB | Google Sheets |
| 백엔드 | Google Apps Script (`Code.gs`) — Web App으로 배포 |
| 프론트엔드 | GitHub 저장소 → GitHub Pages 배포 |
| 핵심 루프 | 미션 뽑기 → 60초 타이머 → 성공/실패 판정 → 랭킹 반영 |
| 차별화 포인트 | 뽑기·타이머·성공·실패 각 구간마다 몰입감 있는 연출(애니메이션+사운드)로 "부스 앞에 사람이 모이는" 스펙터클 구현 |

---

## 1. 기능분석

### 1.1 관리자 모드 — 사전 준비 (`admin.html`, 행사 전 원격 작업용)
- 미션 등록 / 수정 / 삭제 (텍스트, 난이도, 카테고리, 사용여부)
- 게임 설정값 관리 (타이머 초 수, 이벤트명, 관리자 PIN, 재도전 허용 여부 등) — Settings 시트 기반
- 랭킹보드 초기화, 특정 기록 삭제
- 전체 시도 로그(성공/실패 포함) 조회 및 CSV 형태 확인

### 1.2 현장 진행 제어 — 게임 마스터 패널 (부스 운영 중, `index.html` 내 PIN으로 잠금 해제되는 관리자 패널)
- 참가자 이름(닉네임) 입력
- "미션 뽑기" 트리거 → 화면에 뽑기 연출 실행
- "타이머 시작" 버튼 (뽑힌 미션을 참가자가 확인한 뒤 진행자가 직접 시작 — 준비 시간 확보)
- "미션 클리어" / "실패 처리" 버튼 (타이머 진행 중 언제든 판정 가능)
- 재뽑기(패스) 1회 허용 옵션 — 상황상 수행 불가능한 미션이 나왔을 때 대비 (선택 기능으로 제안)

### 1.3 참가자/전시 화면 — 디스플레이 (부스에 설치된 큰 화면/빔프로젝터용)
- 대기(어트랙트) 모드: 이벤트 로고 + 실시간 랭킹보드 자동 롤링
- 미션 뽑기 연출 (긴장감 있는 슬롯/카드 연출)
- 60초 카운트다운 연출 (화려한 애니메이션)
- 성공 연출 (축하) / 실패 연출 (터지는 연출)
- 결과 반영 후 자동으로 대기 모드 복귀 + 랭킹보드 갱신

---

## 2. 시스템 아키텍처

### 2.1 전체 구조

```
[참가자/디스플레이 화면]        [관리자 사전등록 화면]
   index.html (GitHub Pages)      admin.html (GitHub Pages)
        │  fetch(JSON)                  │  fetch(JSON)
        └───────────────┬───────────────┘
                         ▼
         Google Apps Script Web App (exec URL)
              Code.gs — doGet / doPost
                 액션 기반 dispatch 라우팅
                         │
                         ▼
                  Google Sheets (DB)
        Missions / Rankings / GameLog / Settings
```

### 2.2 화면 구성 방식 — 1기기 vs 2기기 (설계 트레이드오프)

| 방식 | 설명 | 장점 | 단점 |
|---|---|---|---|
| **A안 (권장)** 1기기 통합 | 부스의 화면 하나에 `index.html`을 띄우고, 진행자가 PIN으로 관리자 패널을 오버레이로 열어 직접 조작 | 네트워크 동기화 이슈 없음, 배포/운영 단순, 행사 중 장애 위험 최소 | 진행자가 화면 바로 앞에서 조작해야 함(부스 환경상 큰 문제 아님) |
| B안 2기기 분리 | 진행자용 태블릿(컨트롤러) + 관람용 큰 화면(디스플레이)을 분리, Sheets 상태값을 폴링(1~2초 간격)으로 동기화 | 진행자가 화면 밖에서 조작 가능, 더 큰 스펙터클 연출 가능 | GAS 폴링 특성상 약간의 지연 발생, 구조 복잡도·API 호출량 증가 |

→ **1차 개발은 A안으로 진행**, 이후 필요 시 B안으로 확장하는 것을 권장 (7장 확장 아이디어 참고)

### 2.3 통신 방식
- 프론트엔드(GitHub Pages)와 백엔드(GAS Web App)는 **서로 다른 도메인**이므로 `google.script.run`이 아닌 **`fetch()`** 사용
- GAS Web App은 `doGet(e)` (조회성 액션: 미션 목록, 랭킹 조회)와 `doPost(e)` (기록성 액션: 결과 저장, 시트 수정)로 분리
- POST 시 CORS 프리플라이트 회피를 위해 `Content-Type: text/plain;charset=utf-8`로 전송하고, 서버에서 `e.postData.contents`를 JSON.parse
- 모든 요청은 `{ action: "drawMission", payload: {...} }` 형태로 통일 → 서버는 단일 진입점에서 `action` 값으로 분기 (기존 프로젝트들의 dispatch 패턴과 동일한 구조 유지)
- 미션 목록은 `CacheService`로 캐싱(예: 5분) → 뽑기 연출 시 시트 읽기 지연 없이 즉시 반응
- 결과 기록(성공/실패) 저장 시 `LockService`로 동시성 제어 (부스가 2대 이상 운영될 경우 대비)

---

## 3. 구글 시트 DB 구조

### 3.1 `Missions` 시트
| 컬럼 | 설명 |
|---|---|
| ID | 미션 고유 ID |
| 미션내용 | 화면에 표시될 미션 텍스트 |
| 카테고리 | (선택) 예: 몸으로 표현, 말하기, 퀴즈, 협동 |
| 난이도 | (선택) 하/중/상 |
| 사용여부 | TRUE/FALSE — 비활성 미션은 뽑기 대상에서 제외 |
| 등록일시 | 자동 기록 |

### 3.2 `Rankings` 시트 (성공자만 등록)
| 컬럼 | 설명 |
|---|---|
| 순번 | 자동 증가 |
| 참가자명 | 입력한 이름/닉네임 |
| 미션내용 | 수행한 미션 |
| 소요시간(초) | 뽑기~클리어 버튼까지 걸린 시간 → **빠를수록 상위 랭크** (성공 여부만이 아니라 스피드 경쟁 요소 추가) |
| 클리어일시 | 자동 기록 |

### 3.3 `GameLog` 시트 (성공/실패 전체 이력)
| 컬럼 | 설명 |
|---|---|
| 순번 | 자동 증가 |
| 참가자명 | |
| 미션내용 | |
| 결과 | 성공 / 실패 |
| 소요시간(초) | 실패 시 60(만료) 또는 중도 포기 시각 |
| 시도일시 | |

### 3.4 `Settings` 시트 (Key-Value)
| 항목 | 값(예시) |
|---|---|
| 타이머초 | 60 |
| 이벤트명 | 2026 독서행사 미션 챌린지 |
| 관리자PIN | 1234 |
| 재도전허용 | TRUE |

> 초기 실행 시 시트가 없으면 `Code.gs`에서 자동 생성 + 헤더 세팅 (기존 프로젝트들과 동일한 자동 초기화 패턴)

---

## 4. 게임 플로우

1. 참가자가 부스에 도착 → 진행자가 이름 입력
2. 진행자가 "미션 뽑기" 클릭 → 디스플레이 화면에 **뽑기 연출** 실행 → 미션 확정
3. 참가자가 미션 내용을 확인하고 준비 (예: 도구가 필요한 미션이면 세팅)
4. 진행자가 "타이머 시작" 클릭 → **60초 카운트다운 연출** 시작
5. 참가자가 미션 수행, 진행자가 육안으로 판정
6. 시간 내 완료 시 진행자가 "미션 클리어" 클릭 → **성공 연출** + Rankings/GameLog 기록
7. 시간 초과 또는 진행자가 "실패 처리" 클릭 시 → **실패(폭발) 연출** + GameLog만 기록
8. 3초~5초 결과 화면 노출 후 자동으로 **대기(어트랙트) 화면**으로 복귀, 랭킹보드 갱신 표시

---

## 5. 애니메이션 & 연출 컨셉 (핵심 차별화 포인트)

독서 행사라는 맥락을 살려 **"책 속으로 뛰어드는 미션"** 테마를 제안합니다. (테마는 손쉽게 다른 컨셉으로 교체 가능하도록 CSS 변수/이미지 분리 설계 권장)

### 5.1 미션 뽑기 연출
- 책이 화면 중앙에 펼쳐지고, 페이지가 빠르게 팔락팔락 넘어가다가(슬롯머신처럼 미션 후보들이 스쳐 지나가며) 점점 느려지며 한 페이지에 멈춤 → 그 페이지에 미션 텍스트가 잉크로 번지듯 나타남
- 효과음: 책장 넘기는 소리 + 멈출 때 "챙" 효과음

### 5.2 60초 타이머 연출
- 메인: 원형 게이지가 촛불의 촛농처럼 혹은 잉크가 차오르듯 서서히 줄어드는 형태, 초록→노랑→빨강으로 색상 전이
- 마지막 10초: 화면 미세 흔들림 + 배경 붉은 펄스 + 심장박동 사운드로 긴장감 고조
- 숫자는 큼직하게 중앙 표시, 매 초마다 살짝 바운스되는 스케일 애니메이션

### 5.3 성공 연출
- 화면 전체에 색종이/별가루 파티클 폭죽 (canvas 기반)
- "MISSION CLEAR" 금박 도장이 쾅 찍히는 스탬프 애니메이션
- 참가자 이름 + 소요시간이 트로피 카드 형태로 표시 → 자동으로 랭킹보드에 슬라이드인
- 효과음: 팡파레

### 5.4 실패 연출
- 화면이 "펑" 터지는 느낌: 책이 폭발하듯 페이지 조각이 사방으로 흩날리는 파티클 + 화면 크랙(금가는) 오버레이
- 붉은 경고 플래시 2~3회 + 화면 강한 흔들림(shake)
- "MISSION FAILED" 텍스트가 찌그러지듯 등장
- 효과음: 폭발음(과장되지 않게, 코믹한 톤 권장 — 부스 분위기용)

### 5.5 기술 구현 방식
| 요소 | 구현 방법 |
|---|---|
| 파티클(폭죽/폭발) | 경량 커스텀 canvas 파티클 스크립트 또는 `canvas-confetti` CDN 라이브러리 |
| 타이머 게이지 | SVG `stroke-dashoffset` 애니메이션 + CSS 트랜지션 |
| 화면 흔들림/펄스 | CSS `@keyframes` (transform, filter) |
| 사운드 | Web Audio API 또는 `<audio>` 태그, 짧은 mp3/ogg 프리로드 |
| 전체 상태 전환 | 순수 JS 상태머신(`idle → drawing → ready → timing → success/fail → idle`)으로 화면 요소 show/hide 제어 |

---

## 6. API 설계 (액션 기반)

| action | method | 설명 | payload | 응답 |
|---|---|---|---|---|
| `getMissions` | GET | 활성 미션 목록 조회(캐싱) | - | 미션 배열 |
| `getRanking` | GET | 랭킹보드 조회(상위 N) | limit | 랭킹 배열 |
| `getSettings` | GET | 타이머초/이벤트명 등 설정값 조회 | - | 설정 객체 |
| `drawMission` | POST | 미션 뽑기 결과 임시 기록(선택) | participantName | 뽑힌 미션 |
| `submitResult` | POST | 성공/실패 결과 저장 | name, mission, result, seconds | 저장 성공 여부 |
| `addMission` | POST | (관리자) 미션 등록 | mission, category, level | 등록 성공 여부 |
| `updateMission` / `deleteMission` | POST | (관리자) 미션 수정/삭제 | id, ... | 처리 결과 |
| `resetRanking` | POST | (관리자) 랭킹 초기화 | pin | 처리 결과 |
| `updateSettings` | POST | (관리자) 설정 변경 | key, value | 처리 결과 |

> 모든 관리자용 action은 서버 단에서 `pin` 값을 Settings 시트의 관리자PIN과 대조해 검증

---

## 7. 관리자 보안 & 운영 고려사항

- 관리자 패널(현장 제어/사전등록 모두) 접근 시 PIN 입력 요구
- 서버 측에서도 관리자 action은 반드시 PIN 재검증 (클라이언트 검증만으로는 우회 가능)
- 입력값 검증: 참가자 이름 길이 제한, 특수문자/스크립트 삽입 방지(escape 처리)
- `LockService`로 동시 쓰기 충돌 방지 (특히 다중 부스 운영 시)
- 시트 자동 초기화 함수로 최초 배포 시 헤더/기본 Settings 값 자동 세팅
- 행사장 네트워크 불안정 대비: fetch 실패 시 재시도 로직 + 사용자에게 "네트워크 확인" 안내 문구

---

## 8. 배포 방법

### 8.1 GAS 백엔드 배포
1. Apps Script 프로젝트 생성, `Code.gs` 작성 (doGet/doPost + dispatch 라우팅)
2. 배포 → 웹 앱으로 배포: 실행 사용자 "나", 액세스 권한 "모든 사용자"
3. 발급된 `exec` URL을 프론트엔드 설정 파일(`config.js`)에 상수로 고정 (기존 프로젝트 관례와 동일)

### 8.2 GitHub Pages 프론트엔드 배포
1. 저장소 생성 → `index.html`, `admin.html`, `style.css`, `script.js`, `config.js`, 이미지/사운드 에셋 업로드
2. 저장소 Settings → Pages → 배포 브랜치 지정 → 배포 URL 확인
3. `config.js`에 GAS exec URL 입력 후 커밋

### 8.3 현장 운영 체크리스트
- 부스 와이파이/핫스팟 안정성 사전 테스트
- 디스플레이용 대형 화면 또는 빔프로젝터 미러링 확인
- 스피커 연결(효과음 재생용)
- 예비 기기(태블릿/노트북) 준비
- 행사 전 미션 최소 10~15개 이상 등록 권장(반복 노출 방지)

---

## 9. 향후 확장 아이디어

- 2기기 분리(B안)로 전환해 진행자가 화면 밖에서 조작 + 더 큰 화면 연출
- QR코드로 참가자가 자신의 결과/랭킹을 개인 휴대폰에서 확인
- 관람객 실시간 응원 투표(가장 재미있었던 도전자 하이라이트)
- 행사 종료 후 전체 결과 CSV 다운로드(관리자 페이지)
- 연속 성공(콤보) 시 특별 이펙트, 시간대별 이벤트 미션(예: "깜짝 2배 보너스 타임")

---

## 10. 개발 순서 제안 (다음 단계)

1. `Code.gs` 전체 코드 (시트 자동 초기화 + action 라우팅 + LockService/CacheService 적용)
2. `admin.html` (미션 CRUD, 설정 관리)
3. `index.html` + `script.js` + `style.css` (상태머신, 연출 애니메이션, 관리자 오버레이 패널)
4. 로컬 테스트 → GAS 배포 → GitHub Pages 배포 → 실제 화면/기기로 리허설
